# InternVL Training Configuration

DATA_LOADER:
  TRAIN_ID: 'datasets/coco/train_ids.json'
  VAL_ID: 'datasets/coco/val_ids.json'
  TEST_ID: 'datasets/coco/val_ids.json'
  
  TRAIN_GV_FEAT: ''
  VAL_GV_FEAT: ''
  TEST_GV_FEAT: ''
  
  TRAIN_ATT_FEATS: ''
  VAL_ATT_FEATS: ''
  TEST_ATT_FEATS: ''

INFERENCE:
  EVAL: 'COCO'
  VAL_ANNFILE: 'datasets/coco/captions_val.json'
  TEST_ANNFILE: 'datasets/coco/captions_val.json'
  ID_KEY: 'image_id'
  CAP_KEY: 'caption'
  VOCAB: 'coco_vocabulary.txt'

MODEL:
  VOCAB_SIZE: 9487
  SEQ_LEN: 512

# InternVL Specific
SYSTEM_PROMPT: |
  You are an AI assistant that rigorously follows this response protocol:
  
  1. First, conduct a detailed analysis of the question. Consider different angles, potential solutions, and reason through the problem step-by-step. Enclose this entire thinking process within <think> and </think> tags.
  
  2. After the thinking section, provide a clear, concise, and direct answer to the user's question. Separate the answer from the think section with a newline.
  
  Ensure that the thinking process is thorough but remains focused on the query. The final answer should be standalone and not reference the thinking section.

USER_PROMPT: "Describe this image in detail."
